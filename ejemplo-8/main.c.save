#include <stdio.h>
#include <stdlib.h>

float comparar_float(float, float);

int main (int numArgumentos, char ** argumentos){
        printf("Casos de prueba (1: verdadero, 0: falso)\r\n");
        float casos [] [2]={{-0.5, -0.4},{-0.5, 0.4},{0.5, -0.4},{0.5, 0.4},
                            {-0.4, -0.5},{-0.4, 0.5},{0.4, -0.5},{0.4, 0.5}};
        float resultado;

        for(int i=0; i< sizeof(casos)/sizeof(casos[0]);){
	   resultado=comparar
	}

        resultado=comparar_float(numero1, numero2);
        printf("%f + %f = %f", numero1, numero2, resultado);
        exit(EXIT_SUCCESS);
}
